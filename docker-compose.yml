# Convenience runners for the robotics-ws workspace. Not used by CI.
#
#   docker compose run --rm dev          # interactive shell with ROS sourced
#   docker compose up sim                # headless-capable full sim stack
services:
  dev:
    image: robotics-ws:dev
    build:
      context: .
      target: dev
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - GZ_IP=127.0.0.1
      - ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST
    network_mode: host
    ipc: host
    volumes:
      - ./src:/ws/src:rw
    command: bash

  sim:
    image: robotics-ws:dev
    build:
      context: .
      target: dev
    environment:
      - DISPLAY=${DISPLAY:-:0}
      - GZ_IP=127.0.0.1
      - ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST
    network_mode: host
    ipc: host
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    command: >
      ros2 launch robot_description mission.launch.py headless:=false